{
    // by default this should output to only console, have not explicetly set its logging to go elsewhere
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.Hosting.Lifetime": "Information"
        }
    },
    "ConnectionStrings": {
        "DefaultConnection": "Server=localhost;Port=5432;Database=messagingwebappdb;Username=learninguser;Password=learningPassword;"
    },

    "KafkaConfiguration": {
        "Brokers": "localhost:9092",
        "Topic": "userMessages",
        "Key": "my_key", // only 1 topic and 1 partition so, key doesnt actually matter but I wanted to use one for learning anyway
        "ConsumerGroup": "userMessageGroup" // 1 Kafka client, 1 consumer group.
    }
}
